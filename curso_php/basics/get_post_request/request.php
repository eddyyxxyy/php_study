<?php

/** REQUEST
 * 
 * Com o $_REQUEST temos uma funcionalidade
 * diferente, pois ele pega ambos os valores
 * enviados por GET e POST, além de mais alguns
 * detalhes, como por exemplo o PHPSESSID e o
 * token.
 * 
 * ALém disso, ele mostra os valores recebidos
 * na URL, o que o torna menos seguro, assim
 * como o GET ele mostra a query string na URL.
 */
